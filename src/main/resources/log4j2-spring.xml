<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="DEBUG">
  <Properties>
    <Property name="LOG_PATH">../springBootAppLogs/holidayPlannerLogs</Property>
  </Properties>

  <Appenders>
    <!-- Console Appender -->
    <Console name="Console" target="SYSTEM_OUT">
      <PatternLayout pattern="%d{HH:mm:ss.SSS} %-5level : [%t] %c{3}.%M:%L - %msg%n"/>
    </Console>

    <!-- RollingFile Appender without Filter (gets everything) -->
    <RollingFile name="LogFile" fileName="${LOG_PATH}/appLogs.log"
                  filePattern="${LOG_PATH}/appLogs-%d{yyyy-MMM-dd}.log">
      <PatternLayout pattern="%d{dd-MM-yyyy HH:mm:ss.SSS} %-5level : [%t] %c.%M:%L - %msg%n"/>
      <Policies>
        <TimeBasedTriggeringPolicy interval="1"/>
      </Policies>
    </RollingFile>
  </Appenders>

  <Loggers>
    <!-- Application logs -->
    <Logger name="com.arpajit.holidayPlanner" level="INFO" additivity="false">
      <AppenderRef ref="Console"/>
      <AppenderRef ref="LogFile"/>
    </Logger>

    <!-- Default/root logger setup -->
    <Root level="WARN">
      <AppenderRef ref="Console"/>
      <AppenderRef ref="LogFile"/>
    </Root>
  </Loggers>
</Configuration>
