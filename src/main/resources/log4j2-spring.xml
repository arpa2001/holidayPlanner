<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="DEBUG">
  <Properties>
    <Property name="LOG_PATH">logs</Property>
    <Property name="LOG_PATTERN">%d{HH:mm:ss.SSS} %-5level : [%t] %c{3}.%M:%L - %msg%n</Property>
  </Properties>

  <Appenders>
    <!-- Console Appender -->
    <Console name="Console" target="SYSTEM_OUT">
      <PatternLayout pattern="${LOG_PATTERN}"/>
    </Console>

    <!-- RollingFile Appender without Filter (gets everything) -->
    <RollingFile name="LogFile" fileName="${LOG_PATH}/appLogs.log"
                  filePattern="${LOG_PATH}/appLogs-%d{yyyy-MMM-dd}.log">
      <PatternLayout pattern="${LOG_PATTERN}"/>
      <Policies>
        <TimeBasedTriggeringPolicy interval="1"/>
      </Policies>
    </RollingFile>
  </Appenders>

  <Loggers>
    <!-- Application logs -->
    <Logger name="com.arpajit.holidayPlanner" level="DEBUG" additivity="false">
      <AppenderRef ref="Console"/>
      <AppenderRef ref="LogFile"/>
    </Logger>

    <!-- Default/root logger setup -->
    <Root level="WARN">
      <AppenderRef ref="Console"/>
      <AppenderRef ref="LogFile"/>
    </Root>
  </Loggers>
</Configuration>
